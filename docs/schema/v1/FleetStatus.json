{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://drod.dev/refresh/schema/v1/FleetStatus.json",
  "$defs": {
    "AddonsBehindSummary": {
      "properties": {
        "total": {
          "type": "integer"
        },
        "behind": {
          "type": "integer"
        },
        "names": {
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "type": "object",
      "required": [
        "total",
        "behind"
      ],
      "description": "AddonsBehindSummary summarizes addon version posture for a cluster."
    },
    "ClusterStatus": {
      "properties": {
        "name": {
          "type": "string"
        },
        "region": {
          "type": "string"
        },
        "version": {
          "type": "string"
        },
        "support": {
          "$ref": "#/$defs/SupportPosture"
        },
        "compute": {
          "$ref": "#/$defs/ComputeType"
        },
        "nodegroupCount": {
          "type": "integer"
        },
        "staleAmi": {
          "$ref": "#/$defs/StaleAMISummary"
        },
        "addonsBehind": {
          "$ref": "#/$defs/AddonsBehindSummary"
        },
        "nodegroupsBehindControlPlane": {
          "type": "integer",
          "description": "nodegroupsBehindControlPlane counts nodegroups on an older Kubernetes minor than the control plane (a half-finished upgrade). StaleAMI can't show this: AMI freshness is judged against each nodegroup's own minor."
        },
        "healthIssues": {
          "type": "integer",
          "description": "healthIssues is the count of AWS-reported control-plane health issues (DescribeCluster Health.Issues) — degraded resources, IAM failures, etc."
        },
        "incomplete": {
          "type": "boolean",
          "description": "incomplete is true when some of the row's data could not be read (a failed AWS call, or a sweep that stopped first). The row still renders with what was read; its failures are in the document's top-level \"failures\", identified by cluster (or, for the cluster itself, name)."
        }
      },
      "type": "object",
      "required": [
        "name",
        "region",
        "version",
        "support",
        "compute",
        "nodegroupCount",
        "staleAmi",
        "addonsBehind",
        "nodegroupsBehindControlPlane"
      ],
      "description": "ClusterStatus is the fleet-status row for a single cluster."
    },
    "ComputeType": {
      "type": "string",
      "enum": [
        "ManagedNodegroups",
        "AutoMode",
        "Karpenter",
        "None"
      ],
      "description": "ComputeType describes how a cluster provisions its worker nodes. It exists so `refresh status` never renders a nodegroup-less cluster as an empty row."
    },
    "Failure": {
      "properties": {
        "kind": {
          "$ref": "#/$defs/FailureKind"
        },
        "name": {
          "type": "string",
          "description": "name identifies the item. For KindRegion it is the region name."
        },
        "cluster": {
          "type": "string"
        },
        "region": {
          "type": "string"
        },
        "operation": {
          "type": "string",
          "description": "operation is the IAM action that failed, such as \"eks:DescribeNodegroup\"."
        },
        "reason": {
          "$ref": "#/$defs/FailureReason"
        },
        "retryable": {
          "type": "boolean",
          "description": "retryable reports whether running the same command again, with no other change, may succeed. It is always encoded."
        },
        "error": {
          "type": "string",
          "description": "error is one line of human text. Do not parse it; use Reason."
        },
        "awsErrorCode": {
          "type": "string",
          "description": "awsErrorCode is the raw AWS API error code, such as AccessDeniedException, when the failure came from an API response."
        },
        "updateId": {
          "type": "string"
        }
      },
      "type": "object",
      "required": [
        "kind",
        "name",
        "reason",
        "retryable",
        "error"
      ],
      "description": "Failure is one item refresh could not read, or one action it attempted that did not complete. The JSON and YAML keys are a public contract (docs/concepts/output.md): later versions only add fields."
    },
    "FailureKind": {
      "type": "string",
      "enum": [
        "Region",
        "Cluster",
        "Nodegroup",
        "Addon",
        "Insight",
        "Update",
        "PodDisruptionBudget",
        "Node"
      ],
      "description": "FailureKind is the type of item a Failure is about."
    },
    "FailureList": {
      "items": {
        "$ref": "#/$defs/Failure"
      },
      "type": "array",
      "description": "A list of failures. It is [] when there are none, never null."
    },
    "FailureReason": {
      "type": "string",
      "enum": [
        "AccessDenied",
        "CredentialError",
        "Throttled",
        "NotFound",
        "RegionUnavailable",
        "InvalidRequest",
        "ServiceError",
        "NetworkError",
        "Timeout",
        "Interrupted",
        "UpdateFailed",
        "UpdateCancelled",
        "NotMonitored",
        "NotAttempted",
        "Unknown"
      ],
      "description": "FailureReason is why a Failure happened, from a closed set. Scripts branch on it. Later versions may add reasons; consumers must accept unknown values."
    },
    "StaleAMISummary": {
      "properties": {
        "total": {
          "type": "integer"
        },
        "behind": {
          "type": "integer"
        },
        "oldestDays": {
          "type": "integer",
          "description": "oldestDays is the age of the oldest stale AMI in days, when resolvable."
        }
      },
      "type": "object",
      "required": [
        "total",
        "behind"
      ],
      "description": "StaleAMISummary summarizes nodegroup AMI posture for a cluster."
    },
    "SupportPosture": {
      "properties": {
        "tier": {
          "$ref": "#/$defs/SupportTier"
        },
        "standardUntil": {
          "type": "string",
          "format": "date-time"
        },
        "extendedUntil": {
          "type": "string",
          "format": "date-time"
        },
        "daysRemaining": {
          "type": "integer",
          "description": "daysRemaining counts down to the end of the current tier (standard or extended). Negative once the window has closed."
        },
        "extraCostUsdPerHour": {
          "type": "number",
          "description": "extraCostUsdPerHour is the per-cluster premium of the current tier over standard support (0 unless extended)."
        },
        "autoUpgradeAtStandardEnd": {
          "type": "boolean",
          "description": "autoUpgradeAtStandardEnd is true when the cluster's upgrade policy is STANDARD: EKS auto-upgrades it at the end of standard support instead of moving it to (paid) extended support."
        },
        "fallback": {
          "type": "boolean",
          "description": "fallback is true when the posture came from the compiled-in calendar because DescribeClusterVersions was unavailable."
        }
      },
      "type": "object",
      "required": [
        "tier"
      ],
      "description": "SupportPosture is the resolved support window for a cluster's version."
    },
    "SupportTier": {
      "type": "string",
      "enum": [
        "Standard",
        "Extended",
        "Unsupported",
        "Unknown"
      ],
      "description": "SupportTier is the EKS support posture for a cluster's Kubernetes version."
    }
  },
  "properties": {
    "apiVersion": {
      "type": "string",
      "const": "refresh.drod.dev/v1",
      "description": "The version of the document contract."
    },
    "kind": {
      "type": "string",
      "const": "FleetStatus",
      "description": "The type of the document."
    },
    "clusters": {
      "anyOf": [
        {
          "items": {
            "$ref": "#/$defs/ClusterStatus"
          },
          "type": "array"
        },
        {
          "type": "null"
        }
      ]
    },
    "failures": {
      "$ref": "#/$defs/FailureList",
      "description": "Every failure of the run: the items refresh could not read and the actions that did not complete. This top-level list is complete; a nested failure or failures field repeats a subset of it. The list is sorted by kind, region, cluster, name, and operation. [] when there are no failures."
    }
  },
  "type": "object",
  "required": [
    "apiVersion",
    "kind",
    "clusters",
    "failures"
  ],
  "title": "FleetStatus",
  "description": "The document that `refresh status -o json` prints. -o yaml prints the same structure."
}
