{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://drod.dev/refresh/schema/v1/ClusterList.json",
  "$defs": {
    "ClusterSummary": {
      "properties": {
        "name": {
          "type": "string"
        },
        "status": {
          "type": "string"
        },
        "version": {
          "type": "string"
        },
        "region": {
          "type": "string"
        },
        "health": {
          "$ref": "#/$defs/HealthSummary"
        },
        "nodeCount": {
          "$ref": "#/$defs/NodeCountInfo"
        },
        "createdAt": {
          "type": "string",
          "format": "date-time"
        },
        "tags": {
          "additionalProperties": {
            "type": "string"
          },
          "type": "object"
        },
        "incomplete": {
          "type": "boolean",
          "description": "incomplete is true when some of the row's data could not be read (the cluster, its nodegroup list, or a nodegroup). Its failures are in the ClusterList document's \"failures\", identified by cluster (or, for the cluster itself, name)."
        }
      },
      "type": "object",
      "required": [
        "name",
        "status",
        "version",
        "region",
        "nodeCount",
        "createdAt"
      ],
      "description": "ClusterSummary is used for list operations"
    },
    "Decision": {
      "type": "string",
      "enum": [
        "Proceed",
        "Warn",
        "Block"
      ],
      "description": "Decision represents the overall decision for proceeding with update"
    },
    "Failure": {
      "properties": {
        "kind": {
          "$ref": "#/$defs/FailureKind"
        },
        "name": {
          "type": "string",
          "description": "name identifies the item. For KindRegion it is the region name."
        },
        "cluster": {
          "type": "string"
        },
        "region": {
          "type": "string"
        },
        "operation": {
          "type": "string",
          "description": "operation is the IAM action that failed, such as \"eks:DescribeNodegroup\"."
        },
        "reason": {
          "$ref": "#/$defs/FailureReason"
        },
        "retryable": {
          "type": "boolean",
          "description": "retryable reports whether running the same command again, with no other change, may succeed. It is always encoded."
        },
        "error": {
          "type": "string",
          "description": "error is one line of human text. Do not parse it; use Reason."
        },
        "awsErrorCode": {
          "type": "string",
          "description": "awsErrorCode is the raw AWS API error code, such as AccessDeniedException, when the failure came from an API response."
        },
        "updateId": {
          "type": "string"
        }
      },
      "type": "object",
      "required": [
        "kind",
        "name",
        "reason",
        "retryable",
        "error"
      ],
      "description": "Failure is one item refresh could not read, or one action it attempted that did not complete. The JSON and YAML keys are a public contract (docs/concepts/output.md): later versions only add fields."
    },
    "FailureKind": {
      "type": "string",
      "enum": [
        "Region",
        "Cluster",
        "Nodegroup",
        "Addon",
        "Insight",
        "Update",
        "PodDisruptionBudget",
        "Node"
      ],
      "description": "FailureKind is the type of item a Failure is about."
    },
    "FailureList": {
      "items": {
        "$ref": "#/$defs/Failure"
      },
      "type": "array",
      "description": "A list of failures. It is [] when there are none, never null."
    },
    "FailureReason": {
      "type": "string",
      "enum": [
        "AccessDenied",
        "CredentialError",
        "Throttled",
        "NotFound",
        "RegionUnavailable",
        "InvalidRequest",
        "ServiceError",
        "NetworkError",
        "Timeout",
        "Interrupted",
        "UpdateFailed",
        "UpdateCancelled",
        "NotMonitored",
        "NotAttempted",
        "Unknown"
      ],
      "description": "FailureReason is why a Failure happened, from a closed set. Scripts branch on it. Later versions may add reasons; consumers must accept unknown values."
    },
    "HealthResult": {
      "properties": {
        "name": {
          "type": "string"
        },
        "status": {
          "$ref": "#/$defs/HealthStatus"
        },
        "score": {
          "type": "integer",
          "description": "0-100"
        },
        "message": {
          "type": "string"
        },
        "details": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "isBlocking": {
          "type": "boolean"
        },
        "skipped": {
          "type": "boolean",
          "description": "skipped marks a check that could not be evaluated (e.g. no Kubernetes client) rather than measured. Skipped checks are excluded from the OverallScore so a missing prerequisite doesn't silently drag the score."
        }
      },
      "type": "object",
      "required": [
        "name",
        "status",
        "score",
        "message",
        "isBlocking"
      ],
      "description": "HealthResult represents the result of a single health check"
    },
    "HealthStatus": {
      "type": "string",
      "enum": [
        "Pass",
        "Warn",
        "Fail"
      ],
      "description": "HealthStatus represents the status of a health check"
    },
    "HealthSummary": {
      "properties": {
        "results": {
          "items": {
            "$ref": "#/$defs/HealthResult"
          },
          "type": "array"
        },
        "overallScore": {
          "type": "integer"
        },
        "decision": {
          "$ref": "#/$defs/Decision"
        },
        "warnings": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "errors": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "failures": {
          "$ref": "#/$defs/FailureList",
          "description": "failures are the EKS and Kubernetes reads the node and PDB checks could not make. The check that needed the data warns or fails instead of passing, so the Decision already accounts for them. [] when empty."
        }
      },
      "type": "object",
      "required": [
        "results",
        "overallScore",
        "decision",
        "failures"
      ],
      "description": "HealthSummary represents the overall health check results"
    },
    "NodeCountInfo": {
      "properties": {
        "ready": {
          "type": "integer"
        },
        "total": {
          "type": "integer"
        },
        "readyKnown": {
          "type": "boolean"
        }
      },
      "type": "object",
      "required": [
        "ready",
        "total",
        "readyKnown"
      ],
      "description": "NodeCountInfo aggregates node information across nodegroups. Ready is a measured count only when ReadyKnown is true; otherwise only Total (desired capacity) is meaningful."
    }
  },
  "properties": {
    "apiVersion": {
      "type": "string",
      "const": "refresh.drod.dev/v1",
      "description": "The version of the document contract."
    },
    "kind": {
      "type": "string",
      "const": "ClusterList",
      "description": "The type of the document."
    },
    "clusters": {
      "items": {
        "$ref": "#/$defs/ClusterSummary"
      },
      "type": "array"
    },
    "count": {
      "type": "integer"
    },
    "failures": {
      "$ref": "#/$defs/FailureList",
      "description": "Every failure of the run: the items refresh could not read and the actions that did not complete. This top-level list is complete; a nested failure or failures field repeats a subset of it. The list is sorted by kind, region, cluster, name, and operation. [] when there are no failures."
    }
  },
  "type": "object",
  "required": [
    "apiVersion",
    "kind",
    "clusters",
    "count",
    "failures"
  ],
  "title": "ClusterList",
  "description": "The document that `refresh cluster list -o json` prints. -o yaml prints the same structure."
}
